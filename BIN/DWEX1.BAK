#include<iostream.h>
#include<conio.h>
int main()
{
 clrscr();
 int num,sum,temp;
 cout<<"Input a number: ";
 cin>>num;
 temp = num;
 sum = 0;
 do
 {
  sum = sum + num;
  num--;
 }
 while(num > 0);
 cout<<"The sum of the numbers from 1 to "<< temp << " is " << sum;
 getch();
 return 0;
}
