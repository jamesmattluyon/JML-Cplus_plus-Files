#include<iostream.h>
#include<conio.h>
int main()
{
 clrscr();
 int x,sum;
 sum = 0;
 for(x = 1; x <= 100; x++)
 {
  sum = sum + x;
 }
  cout<<"The sum of integers from 1 to 100 is " << sum;
 getch();
 return 0;
}